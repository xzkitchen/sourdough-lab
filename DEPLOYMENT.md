# 🚀 Sourdough Lab - 部署教程

## 📋 前置准备

### 1. 安装 Node.js

- 访问 https://nodejs.org
- 下载并安装 LTS 版本（推荐 18.x 或 20.x）
- 验证安装：
  ```bash
  node -v  # 应显示版本号，如 v20.10.0
  npm -v   # 应显示版本号，如 10.2.3
  ```

### 2. 注册账号

#### GitHub（必需）
- 访问 https://github.com
- 注册免费账号

#### Vercel（推荐）
- 访问 https://vercel.com
- 用 GitHub 账号登录

---

## 🎯 方案A：Vercel 部署（最简单，推荐）

### 步骤1：上传到 GitHub

1. **解压项目文件**
   - 解压 `sourdough-lab.zip`

2. **初始化 Git**
   ```bash
   cd sourdough-lab
   git init
   git add .
   git commit -m "Initial commit"
   ```

3. **创建 GitHub 仓库**
   - 访问 https://github.com/new
   - Repository name 填: `sourdough-lab`
   - 选择 Public
   - **不要**勾选任何初始化选项
   - 点击 "Create repository"

4. **推送代码**
   ```bash
   git remote add origin https://github.com/你的用户名/sourdough-lab.git
   git branch -M main
   git push -u origin main
   ```

### 步骤2：部署到 Vercel

1. **访问 Vercel**
   - 打开 https://vercel.com
   - 用 GitHub 账号登录

2. **导入项目**
   - 点击 "Add New..." → "Project"
   - 在列表中找到 `sourdough-lab`
   - 点击 "Import"

3. **配置（自动识别）**
   - Framework Preset: Vite ✅ (自动识别)
   - Build Command: `npm run build` ✅
   - Output Directory: `dist` ✅
   - 保持默认，点击 "Deploy"

4. **等待部署**
   - 大约 1-2 分钟
   - 看到 "Congratulations!" 就成功了！

5. **访问你的网站**
   - 点击显示的链接
   - 例如：`https://sourdough-lab.vercel.app`

### 🎉 完成！

---

## 🎯 方案B：Netlify 部署（同样简单）

### 方式1：拖拽部署（最快）

1. **构建项目**
   ```bash
   cd sourdough-lab
   npm install
   npm run build
   ```

2. **部署**
   - 访问 https://app.netlify.com/drop
   - 把 `dist` 文件夹拖进去
   - 等待 30 秒
   - 完成！

### 方式2：GitHub 连接（推荐）

1. **上传到 GitHub**（同方案A的步骤1）

2. **连接 Netlify**
   - 访问 https://app.netlify.com
   - 点击 "Add new site" → "Import an existing project"
   - 选择 "GitHub"
   - 授权并选择 `sourdough-lab` 仓库

3. **配置**
   - Build command: `npm run build`
   - Publish directory: `dist`
   - 点击 "Deploy site"

4. **完成！**

---

## 📱 添加到手机主屏幕

### iPhone / iPad

1. 用 Safari 打开你的网站
2. 点击底部"分享"按钮
3. 向下滑，找到"添加到主屏幕"
4. 点击"添加"
5. 桌面上会出现图标

### Android

1. 用 Chrome 打开你的网站
2. 点击右上角三个点
3. 选择"添加到主屏幕"
4. 确认
5. 完成！

---

## 🔄 如何更新网站

### 如果用了 GitHub + Vercel/Netlify

1. **修改代码**
2. **提交并推送**
   ```bash
   git add .
   git commit -m "更新说明"
   git push
   ```
3. **自动部署**
   - Vercel/Netlify 会自动检测
   - 1-2 分钟后就能看到更新

### 如果用了拖拽部署

1. **重新构建**
   ```bash
   npm run build
   ```
2. **重新拖拽**
   - 把新的 `dist` 文件夹拖到 Netlify

---

## ❓ 常见问题

### Q: 构建失败怎么办？

**A:** 检查 Node.js 版本
```bash
node -v  # 确保是 v18+ 或 v20+
```

如果版本过低，更新 Node.js 后重试。

### Q: 部署后页面空白？

**A:** 
1. 等待 2-3 分钟，有时需要时间
2. 强制刷新：Ctrl+F5 (Windows) 或 Cmd+Shift+R (Mac)
3. 清除浏览器缓存

### Q: 手机上添加图标失败？

**A:** 
- iPhone 必须用 Safari
- Android 必须用 Chrome
- 其他浏览器可能不支持 PWA

### Q: 想绑定自己的域名？

**A:** 
1. 购买域名（阿里云、腾讯云等）
2. 在 Vercel/Netlify 项目设置里添加域名
3. 按照提示配置 DNS
4. 等待生效（5-30 分钟）

### Q: 想修改配方数据？

**A:** 
1. 编辑 `src/data/steps.js`
2. 提交并推送到 GitHub
3. 自动部署新版本

### Q: 本地开发看不到效果？

**A:** 
```bash
# 确保安装了依赖
npm install

# 启动开发服务器
npm run dev

# 如果端口被占用
npm run dev -- --port 3000
```

---

## 🎯 性能优化提示

### 已经做的优化

✅ 代码分割 - 按需加载  
✅ Tree Shaking - 移除未使用代码  
✅ 压缩混淆 - 减小文件体积  
✅ CDN 加速 - 全球访问快速  
✅ 字体优化 - 按需加载字体  
✅ PWA 支持 - 离线可用  

### Lighthouse 评分目标

- Performance: 95+  
- Accessibility: 100  
- Best Practices: 100  
- SEO: 100  
- PWA: ✅  

---

## 📞 需要帮助？

如果遇到任何问题：

1. 检查本文档的"常见问题"部分
2. 查看项目的 README.md
3. 截图错误信息并寻求帮助

---

祝你部署顺利！🎉
